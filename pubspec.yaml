name: calvi
description: "Трекер калорій із помічником: людина пише словами, застосунок рахує."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.8+8

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Переклад. Дає не тільки самі рядки, а й множину: українська має три форми
  # («1 запис, 2 записи, 5 записів»), англійська дві, і саморобне рішення
  # ламається саме на цьому.
  flutter_localizations:
    sdk: flutter
  intl: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.1
  flutter_svg: ^2.3.0
  url_launcher: ^6.3.2
  camera: ^0.12.0+2
  image_picker: ^1.2.3

  # The local database. Drift rather than raw sqflite: the schema, the queries
  # and the migrations are checked at build time, and this is the code where a
  # silent mistake costs somebody their history.
  drift: ^2.31.0
  drift_flutter: ^0.2.8
  path_provider: ^2.1.5
  # Ids are made on the phone, so an entry written offline keeps its identity
  # when it finally reaches the server.
  uuid: ^4.5.1
  # The one call the app makes to its own server. Nothing else talks to the
  # network, so a client rather than a framework.
  http: ^1.2.2
  mobile_scanner: ^7.4.0
  speech_to_text: ^7.4.0
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  package_info_plus: ^10.2.1
  google_sign_in: ^7.2.0

# The iOS camera plugin drags in objective_c, and objective_c 9.3 upwards ships
# build hooks, which the Dart 3.10 toolchain refuses to compile a build script
# against: code generation stops working for the whole project. iOS is outside
# the MVP anyway (no Mac to build it on), so the older one is pinned here and
# nothing on Android notices.
# path_provider's Apple implementation pulls objective_c, which ships build
# hooks; the Dart 3.10 toolchain then refuses to compile any build script for
# this project, and code generation stops for everyone. Pinned to the last
# version that does without them. Nothing on Android is affected, and iOS is
# outside the MVP because there is no Mac to build it on.
dependency_overrides:
  path_provider_foundation: 2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.31.0
  build_runner: ^2.15.1

  # Двигун розпізнавання підмінюється в тестах через свій платформний інтерфейс.
  # Інакше диктування не перевірити взагалі: паузу в голосі в тесті не
  # помовчиш, а вся його поведінка тримається саме на тому, що робиться на цій
  # паузі.
  speech_to_text_platform_interface: ^2.4.0
  plugin_platform_interface: ^2.1.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # Класи перекладу збираються самі при кожній збірці з `lib/l10n/app_*.arb`.
  # Ті файли теж не пишуться руками: їх зліплює `node tools/l10n.mjs` із
  # `l10n/<екран>.<мова>.arb`, щоб не тримати один файл на весь застосунок.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Службові звуки початку і кінця запису голосу. Тільки для iOS: на Android їх
  # грає сама система розпізнавання. Синтезуються `tools/earcons.mjs`.
  assets:
    - assets/sounds/

  # Onest, bundled. google_fonts fetched it over the network at runtime, which
  # on a phone without a connection quietly fell back to Roboto.
  fonts:
    - family: Onest
      fonts:
        - asset: assets/fonts/Onest-Regular.ttf
          weight: 400
        - asset: assets/fonts/Onest-Medium.ttf
          weight: 500
        - asset: assets/fonts/Onest-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Onest-Bold.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
